import random, copy

def generate(data):
    data['params']['names_for_user'] = [
    ]
    data['params']['names_from_user'] = [
        {'name': 'longestWord', 'description': 'Return the longest word in a list', 'type': 'function'},
    ]
